# build output
dist/

# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# Local Netlify folder
.netlify

# Local agent scratch. CLAUDE.md and docs/adr/ are deliberately NOT ignored:
# they carry the project rules and the decisions behind them.
.claude/

CONTEXT.md

# Writing backlog: client-permission status and candid distribution notes, in a
# public repo. Working material, not a decision record — hence here, not docs/.
WRITING.md

# Impeccable product record: restates the craftomancer border and cadence
# targets from WRITING.md in full candour, in a public repo. Same reasoning,
# same fate.
PRODUCT.md

# Strategy review over WRITING.md: same cadence targets, same candour about
# them, same public repo. Same fate. Fold it into WRITING.md and delete it if
# it earns its keep.
STRATEGY.md

# Impeccable live-mode config: local agent tooling, same family as .claude/.
.impeccable/
