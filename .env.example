# RAWG API key, used by `pnpm covers` to look up game cover art.
# Authoring-time only: the build never calls out to the network, so deploys
# don't need this set. Get a free key at https://rawg.io/apidocs
RAWG_API_KEY=your_key_here
